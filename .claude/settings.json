{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/karim/Documents/netwrix-intelligence-dashboard && git add -A && git diff --cached --quiet || (git commit -m \"auto-sync $(date +%H:%M:%S)\" && git push --quiet) 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
